public interface IDamagable
{
    /// <summary>
    /// Contract for reducing health of character 
    /// </summary>
    /// <param name="damage"></param>
    private void TakeDamage(float damage) {}
}
